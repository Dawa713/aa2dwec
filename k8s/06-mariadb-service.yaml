apiVersion: v1
kind: Service
metadata:
  name: mariadb
  namespace: tienda-api
spec:
  type: ClusterIP
  selector:
    app: mariadb
  ports:
  - name: mysql
    port: 3306
    targetPort: 3306
    protocol: TCP
  # Headless service para StatefulSet
  clusterIP: None
